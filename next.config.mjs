/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.shutterstock.com",
      "img.freepik.com",
      "https://www.istockphoto.com",
    ],
  },
};

export default nextConfig;
